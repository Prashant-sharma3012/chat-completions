export class AgentOneTools {
    addition({a, b}: any): number {
        return a + b
    }

    subtraction({a, b}: any): number {
        return a - b
    }

    multiplication({a, b}: any): number {
        return a * b
    }

    division({a, b}: any): number {
        return a / b
    }
}